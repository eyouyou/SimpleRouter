import 'package:flutter/material.dart';

class Ps5Page extends StatelessWidget {
  const Ps5Page({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Text("PS5游戏");
  }
}
